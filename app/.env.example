# Copier en .env.local (non commite) et renseigner avec les valeurs du projet Supabase.
# Ces deux valeurs sont PUBLIQUES par conception (embarquees dans le bundle front) :
# la cle anon ne donne aucun acces aux tables (RLS + revoke des privileges).
VITE_SUPABASE_URL=https://votre-ref.supabase.co
VITE_SUPABASE_ANON_KEY=votre-cle-anon-publique
